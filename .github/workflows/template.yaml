# GitHub Actions 工作流配置文件
# 此工作流用于自动编译 OpenWrt 固件，基于 example.config 配置文件
# example.config 包含了完整的 OpenWrt 配置选项，定义了固件的功能特性
# 如目标架构、内核版本、预装软件包、网络服务和硬件驱动等

# 工作流名称，显示在 GitHub Actions 中。
name: openwrt-build固件云编译

on:
  workflow_dispatch:
    inputs:
      force-build:
        description: "无论如何构建新固件"
        required: false
        default: "false"
      ssh:
        description: "连接到Actions的SSH-例如你可以自己构建固件"
        required: false
        default: "false"
      # 设置固件默认地址
      ip:
        description: "固件默认地址 (例如: 192.168.1.1)"
        required: true  # 如果你希望这是必填项
        type: string
        default: "192.168.1.77"  # 可选，默认 IP 地址
# 取消注释以下内容以定期运行。注意这里的 cron 表达式不包含秒。
#  schedule:
#    - cron: 0 */18 * * *

env:
  # REPO 表示你想要在此工作流中构建的 OpenWrt 仓库。
  # 你可以修改 REPO_NAME 为你想要的任何名称，但请确保它与其他工作流中的名称不同。
  REPO_NAME: openwrt
  REPO_URL: https://github.com/openwrt/openwrt.git
  REPO_BRANCH: v24.10.0
  BUILD_DEPENDS: "libncurses-dev gawk flex quilt libssl-dev rsync \
    zlib1g-dev unzip time bc bison gcc-multilib g++-multilib \
    gettext texinfo u-boot-tools cpio dosfstools kmod \
    libelf-dev libssl-dev pkg-config device-tree-compiler protobuf-c-compiler libprotobuf-c-dev"
  # CONFIG_FILE 是你自己的编译配置文件的名称，你应该将这些文件上传到工作流仓库的根目录。
  CONFIG_FILE: example.config
  # CUSTOM_SCRIPT_1 是你在更新和安装 feeds 之前要执行的额外 Bash 脚本的名称。
  # CUSTOM_SCRIPT_2 是你在更新和安装 feeds 之后要执行的额外 Bash 脚本的名称。
  # 如果你需要通过这两个脚本来修改固件的初始设置，请将脚本上传到工作流仓库的根目录。
  CUSTOM_SCRIPT_1: example-custom-script-1.sh
  CUSTOM_SCRIPT_2: example-custom-script-2.sh
  # STORE_PRODUCTS 确定是否将所有编译产物上传到工作流制品。
  # 不仅仅是固件，还包括所有编译但未固化到固件中的包。
  STORE_PRODUCTS: false
  # STORE_FIRMWARE 确定是否将固件上传到工作流制品。
  STORE_FIRMWARE: true
  # 以下选项确定固件需要传输的位置。
  COWTRANSFER_FIRMWARE: false
  WETRANSFER_FIRMWARE: false
  RELEASE_FIRMWARE: true
  # 更改为你的时区。
  TIME_ZONE: Asia/Shanghai

jobs:
  check:
    name: 检查源代码更新
    runs-on: ubuntu-latest
    outputs:
      cache-hit: ${{ steps.cache-hash.outputs.cache-hit }}

    steps:
      - name: 获取提交哈希
        id: get-hash
        run: |
          git clone --depth 1 --branch $REPO_BRANCH --single-branch $REPO_URL ./
          echo "commit-hash=$(git rev-parse HEAD)" >> $GITHUB_OUTPUT

      - name: 比较提交哈希
        id: cache-hash
        uses: actions/cache@main
        with:
          path: .${{ env.REPO_NAME }}-${{ env.REPO_BRANCH }}-commit-hash
          key: HEAD-${{ steps.get-hash.outputs.commit-hash }}

      - name: 保存新的提交哈希
        if: steps.cache-hash.outputs.cache-hit != 'true'
        run: |
          echo ${{ steps.get-hash.outputs.commit-hash }} | 
            tee .$REPO_NAME-$REPO_BRANCH-commit-hash

  build:
    name: 构建固件
    runs-on: ubuntu-latest
    needs: check
    permissions:
      contents: write
    if: needs.check.outputs.cache-hit != 'true' || github.event.inputs.force-build == 'true'

    steps:
      - name: 释放磁盘空间
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: false

      - name: 合并磁盘空间
        uses: easimon/maximize-build-space@master
        with:
          swap-size-mb: 1024
          temp-reserve-mb: 100
          root-reserve-mb: 1024

      - name: 初始化环境
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          # 清理旧的源列表
          sudo rm -rf /etc/apt/sources.list.d/*

          # 添加官方源和必要的第三方源
          echo "deb http://archive.ubuntu.com/ubuntu $(lsb_release -sc) main universe multiverse" | sudo tee /etc/apt/sources.list
          echo "deb http://archive.ubuntu.com/ubuntu $(lsb_release -sc)-updates main universe multiverse" | sudo tee -a /etc/apt/sources.list
          sudo -E apt update -y
          sudo -E apt upgrade -y
          sudo -E apt-get -y install $BUILD_DEPENDS
          sudo -E apt-get -y autoremove --purge
          sudo -E apt-get -y clean
          sudo -E apt-get install -y clang
          sudo timedatectl set-timezone $TIME_ZONE


      - name: 检出代码
        uses: actions/checkout@main

      - name: 克隆源代码
        run: |
          df -hT $PWD
          git clone --depth 1 --branch $REPO_BRANCH --single-branch $REPO_URL openwrt
          cd openwrt
          echo "OPENWRT_PATH=$PWD" >> $GITHUB_ENV
          COMMIT_AUTHOR=$(git show -s --date=short --format="作者: %an")
          echo "OPENWRT_PATH=$PWD" >> $GITHUB_ENV
          COMMIT_AUTHOR=$(git show -s --date=short --format="作者: %an")
          echo "COMMIT_AUTHOR=$COMMIT_AUTHOR" >> $GITHUB_ENV
          COMMIT_DATE=$(git show -s --date=short --format="时间: %ci")
          echo "COMMIT_DATE=$COMMIT_DATE" >> $GITHUB_ENV
          COMMIT_MESSAGE=$(git show -s --date=short --format="内容: %s")
          echo "COMMIT_MESSAGE=$COMMIT_MESSAGE" >> $GITHUB_ENV
          COMMIT_HASH=$(git show -s --date=short --format="hash: %H")
          echo "COMMIT_HASH=$COMMIT_HASH" >> $GITHUB_ENV


      - name: 运行自定义脚本1
        run: |
          chmod +x $GITHUB_WORKSPACE/$CUSTOM_SCRIPT_1
          cd openwrt/
          $GITHUB_WORKSPACE/$CUSTOM_SCRIPT_1


      - name: 更新 feeds
        run: cd openwrt/ && ./scripts/feeds update -a

      - name: 安装 feeds
        run: cd openwrt/ && ./scripts/feeds install -a

      - name: 加载自定义配置并生成配置摘要
        # 此步骤处理 example.config 配置文件中的各项设置
        # 并动态生成配置摘要以供后续步骤使用
        run: |
          [ -e files ] && mv $GITHUB_WORKSPACE/files/ openwrt/files/
          [ -e $CONFIG_FILE ] && mv $GITHUB_WORKSPACE/$CONFIG_FILE openwrt/.config
          cd openwrt/
          make defconfig > /dev/null 2>&1
          SOURCE_REPO="$(echo $REPO_URL | awk -F '/' '{print $(NF)}')"
          echo "SOURCE_REPO=$SOURCE_REPO" >> $GITHUB_ENV
          DEVICE_TARGET=$(cat .config | grep CONFIG_TARGET_BOARD | awk -F '"' '{print $2}')
          echo "DEVICE_TARGET=$DEVICE_TARGET" >> $GITHUB_ENV
          DEVICE_SUBTARGET=$(cat .config | grep CONFIG_TARGET_SUBTARGET | awk -F '"' '{print $2}')
          echo "DEVICE_SUBTARGET=$DEVICE_SUBTARGET" >> $GITHUB_ENV
          sed -i "s/192.168.1.1/${{ github.event.inputs.ip }}/g" package/base-files/files/bin/config_generate
          
          # 生成配置摘要文件
          echo "## 配置摘要" > ../config_summary.md
          echo "- 目标架构: $(grep '^CONFIG_TARGET_' .config | grep '=y' | head -5 | tr '\n' ',' | sed 's/,$//')" >> ../config_summary.md
          echo "- 内核版本: $(grep '^CONFIG_LINUX_' .config | grep '=y' | tr '\n' ',' | sed 's/,$//')" >> ../config_summary.md
          echo "- 网络服务: $(grep -E '^CONFIG_DEFAULT_(dnsmasq|firewall4|dropbear|ppp|odhcpd)' .config | grep '=y' | sed 's/CONFIG_DEFAULT_//' | sed 's/=y//' | tr '\n' ',' | sed 's/,$//')" >> ../config_summary.md
          echo "- 网络驱动: $(grep -E '^CONFIG_DEFAULT_kmod-(e1000|igb|r8169|tg3|bnx2|forcedeth)' .config | grep '=y' | sed 's/CONFIG_DEFAULT_//' | sed 's/=y//' | tr '\n' ',' | sed 's/,$//')" >> ../config_summary.md
      - name: 运行自定义脚本2
        run: |
          chmod +x $GITHUB_WORKSPACE/$CUSTOM_SCRIPT_2
          cd openwrt/
          $GITHUB_WORKSPACE/$CUSTOM_SCRIPT_2


      - name: SSH 连接到 Actions
        uses: P3TERX/ssh2actions@main
        if: github.event.inputs.ssh == 'true' || contains(github.event.action, 'ssh')
        env:
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}

      - name: 下载软件包
        id: download-packages
        run: |
          cd openwrt/
          make defconfig
          make download -j8
          find dl -size -1024c -exec ls -l {} \;
          find dl -size -1024c -exec rm -f {} \;


      - name: 编译固件
        id: compile
        run: |
          cd openwrt/
          echo -e "$(nproc) thread compile"
          make -j$(nproc) || make -j1 || make -j1 V=s
          echo "status=success" >> $GITHUB_OUTPUT
          grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/' > DEVICE_NAME
          [ -s DEVICE_NAME ] && echo "DEVICE_NAME=_$(cat DEVICE_NAME)" >> $GITHUB_ENV
          echo "DATE=$(date +"%Y-%m-%d %H:%M:%S")" >> $GITHUB_ENV
          echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV


      - name: 检查空间使用情况
        if: (!cancelled())
        run: df -hT

      - name: 上传所有编译产物
        uses: actions/upload-artifact@main
        if: steps.compile.outputs.status == 'success' && env.STORE_PRODUCTS == 'true'
        with:
          name: OpenWrt_bin${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
          path: openwrt/bin/

      - name: 组织固件相关文件
        id: organize-files
        run: |
          cd openwrt/bin/targets/*/*
          rm -rf packages/
          echo "FIRMWARE=$PWD" >> $GITHUB_ENV
          echo "status=success" >> $GITHUB_OUTPUT

      - name: 准备动态配置说明
        run: |
          cd openwrt/
          # 生成完整的发布说明，包含标准信息和动态配置详情
          {
          echo "**这是为 ${{ env.DEVICE_TARGET }} 编译的OpenWrt固件**"
          echo "### 📒 固件信息"
          echo "- 💻 平台架构: ${{ env.DEVICE_TARGET }}-${{ env.DEVICE_SUBTARGET }}"
          echo "- ⚽ 固件源码: ${{ env.REPO_URL }}"
          echo "- 💝 源码分支: ${{ env.REPO_BRANCH }}"
          echo "- 🚀 内核版本: ${{ env.KERNEL }}"
          echo "- 🌐 默认地址: ${{ github.event.inputs.ip }}"
          echo "### 🧊 固件版本"
          echo "- 固件编译前最后一次➦[主源码](${{ env.REPO_URL }})更新记录"
          echo "- ${{ env.COMMIT_AUTHOR }}"
          echo "- ${{ env.COMMIT_DATE }}"
          echo "- ${{ env.COMMIT_MESSAGE }}"
          echo "- ${{ env.COMMIT_HASH }}"
          echo ""
          echo "### 🔧 实际配置详情"
          
          # 提取目标架构信息
          TARGETS=$(grep '^CONFIG_TARGET_' .config | grep '=y' | head -5 | tr '\n' ', ' | sed 's/, $//')
          echo "- 目标架构: $TARGETS"
          
          # 提取内核版本
          KERNEL=$(grep '^CONFIG_LINUX_' .config | grep '=y' | tr '\n' ', ' | sed 's/, $//')
          echo "- 内核版本: $KERNEL"
          
          # 提取系统基础包
          BASE_SYSTEM=$(grep -E '^CONFIG_DEFAULT_(base-files|libc|libgcc|musl|procd|ubus|uci|uclient-fetch|urandom-seed|urngd|fstools|mtd|netifd|swconfig|uboot-envtools|ucron|uhttpd)' .config | grep '=y' | sed 's/CONFIG_DEFAULT_//' | sed 's/=y//' | tr '\n' ', ' | sed 's/, $//')
          echo "- 基础系统: $BASE_SYSTEM"
          
          # 提取网络服务
          NETWORK=$(grep -E '^CONFIG_DEFAULT_(dnsmasq|firewall4|dropbear|ppp|ppp-mod-pppoe|odhcpd|odhcp6c|firewall|resolveip|kmod-ipt-offload|ca-bundle)' .config | grep '=y' | sed 's/CONFIG_DEFAULT_//' | sed 's/=y//' | tr '\n' ', ' | sed 's/, $//')
          echo "- 网络服务: $NETWORK"
          
          # 提取网络驱动
          DRIVERS=$(grep -E '^CONFIG_DEFAULT_kmod-(e1000|igb|r8169|tg3|bnx2|forcedeth|ath|mt76|rt|atl1c|ixgbe|igc|kmod-usb-net|kmod-usb-net-asix|kmod-usb-net-rtl8152)' .config | grep '=y' | sed 's/CONFIG_DEFAULT_//' | sed 's/=y//' | tr '\n' ', ' | sed 's/, $//')
          echo "- 网络驱动: $DRIVERS"
          
          # 提取无线网络相关
          WIFI=$(grep -E '^CONFIG_DEFAULT_(hostapd|wpad-mini|wpad|kmod-cfg80211|kmod-mac80211|kmod-ath9k|kmod-ath10k|kmod-rt2800|kmod-rt73-usb)' .config | grep '=y' | sed 's/CONFIG_DEFAULT_//' | sed 's/=y//' | tr '\n' ', ' | sed 's/, $//')
          echo "- 无线网络: $WIFI"
          
          # 提取存储和文件系统
          STORAGE=$(grep -E '^CONFIG_DEFAULT_(e2fsprogs|mkf2fs|partx-utils|f2fs-tools|kmod-fs-vfat|kmod-fs-ext4|kmod-fs-ntfs|kmod-usb-storage|kmod-usb-storage-extras|kmod-usb3|kmod-usb2)' .config | grep '=y' | sed 's/CONFIG_DEFAULT_//' | sed 's/=y//' | tr '\n' ', ' | sed 's/, $//')
          echo "- 存储支持: $STORAGE"
          
          # 提取安全功能
          SECURITY=$(grep -E '^CONFIG_DEFAULT_(firewall4|nftables|kmod-ipt-nat|kmod-nf-nathelper-ct|kmod-tun|kmod-udptunnel6|openvpn|openvpn-easy-rsa)' .config | grep '=y' | sed 's/CONFIG_DEFAULT_//' | sed 's/=y//' | tr '\n' ', ' | sed 's/, $//')
          echo "- 安全功能: $SECURITY"
          
          # 提取容器和虚拟化支持
          CONTAINER=$(grep -E '^CONFIG_DEFAULT_(docker|dockerd|containerd|runc|lxc|lxc-attach|kmod-virtio|kmod-virtio-blk|kmod-virtio-console|kmod-virtio-net)' .config | grep '=y' | sed 's/CONFIG_DEFAULT_//' | sed 's/=y//' | tr '\n' ', ' | sed 's/, $//')
          [ -n "$CONTAINER" ] && echo "- 容器支持: $CONTAINER" || echo "- 容器支持: 无"
          
          # 提取开发工具
          DEV_TOOLS=$(grep -E '^CONFIG_DEFAULT_(gcc|gdb|strace|tcpdump|iperf3|screen|tmux|vim-full|nano)' .config | grep '=y' | sed 's/CONFIG_DEFAULT_//' | sed 's/=y//' | tr '\n' ', ' | sed 's/, $//')
          [ -n "$DEV_TOOLS" ] && echo "- 开发工具: $DEV_TOOLS" || echo "- 开发工具: 无"
          
          # 提取LuCI Web界面组件
          LUCI=$(grep -E '^CONFIG_DEFAULT_(luci|luci-base|luci-mod-admin-full|luci-theme-bootstrap|luci-theme-openwrt-2020|luci-app-firewall|luci-app-opkg|luci-proto-ipv6|luci-proto-ppp)' .config | grep '=y' | sed 's/CONFIG_DEFAULT_//' | sed 's/=y//' | tr '\n' ', ' | sed 's/, $//')
          [ -n "$LUCI" ] && echo "- LuCI界面: $LUCI" || echo "- LuCI界面: 无"
          
          # 提取系统性能和监控
          MONITORING=$(grep -E '^CONFIG_DEFAULT_(htop|iftop|vnstat|vnstati|iotop|dstat|kmod-ledtrig-heartbeat|kmod-ledtrig-gpio)' .config | grep '=y' | sed 's/CONFIG_DEFAULT_//' | sed 's/=y//' | tr '\n' ', ' | sed 's/, $//')
          [ -n "$MONITORING" ] && echo "- 性能监控: $MONITORING" || echo "- 性能监控: 无"
          
          # 统计总数
          TOTAL_PKGS=$(grep '^CONFIG_DEFAULT_' .config | grep '=y' | wc -l)
          TOTAL_DRIVERS=$(grep '^CONFIG_DEFAULT_kmod-' .config | grep '=y' | wc -l)
          echo ""
          echo "### 📊 配置统计"
          echo "- 总计启用软件包: $TOTAL_PKGS 个"
          echo "- 总计启用驱动: $TOTAL_DRIVERS 个"
          } > ../dynamic_config_notes.md


      - name: 上传固件目录
        uses: actions/upload-artifact@main
        if: steps.organize-files.outputs.status == 'success' && env.STORE_FIRMWARE == 'true' && !cancelled()
        with:
          name: ${{ env.SOURCE_REPO }}-firmware-${{ env.DEVICE_TARGET }}-${{ env.DEVICE_SUBTARGET }}-${{ env.FILE_DATE }}
          path: ${{ env.FIRMWARE }}

      - name: 上传固件到 Cowtransfer
        id: cowtransfer
        if: steps.organize-files.outputs.status == 'success' && env.COWTRANSFER_FIRMWARE == 'true' && !cancelled()
        run: |
          curl -fsSL git.io/file-transfer | sh
          ./transfer cow --block 2621440 -s -p 64 --no-progress ${FIRMWARE} 2>&1 | tee cowtransfer.log
          echo "::notice file=cowtransfer.com::$(cat cowtransfer.log | grep https)"
          echo "url=$(cat cowtransfer.log | grep https | cut -f3 -d" ")" >> $GITHUB_OUTPUT


      - name: 上传固件到 WeTransfer
        id: wetransfer
        if: steps.organize-files.outputs.status == 'success' && env.WETRANSFER_FIRMWARE == 'true' && !cancelled()
        run: |
          curl -fsSL git.io/file-transfer | sh
          ./transfer wet -s -p 16 --no-progress ${FIRMWARE} 2>&1 | tee wetransfer.log
          echo "::notice file=wetransfer.com::$(cat wetransfer.log | grep https)"
          echo "url=$(cat wetransfer.log | grep https | cut -f3 -d" ")" >> $GITHUB_OUTPUT


      - name: 上传固件到Release
        if: steps.organize-files.outputs.status == 'success' && !cancelled()
        uses: ncipollo/release-action@v1
        with:
          name: R${{ env.DATE }} for ${{ env.DEVICE_TARGET }}
          allowUpdates: true
          tag: ${{ env.DEVICE_TARGET }}
          token: ${{ secrets.GITHUB_TOKEN }}
          artifacts: ${{ env.FIRMWARE }}/*
          bodyFile: dynamic_config_notes.md


  clean:
    name: 清理旧资产
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: write

    steps:
      - name: 删除工作流运行
        uses: Mattraks/delete-workflow-runs@main
        with:
          retain_days: 1
          keep_minimum_runs: 1

      - name: 移除旧版本
        uses: dev-drprasad/delete-older-releases@master
        if: env.RELEASE_FIRMWARE == 'true' && !cancelled()
        with:
          keep_latest: 3
          delete_tags: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
