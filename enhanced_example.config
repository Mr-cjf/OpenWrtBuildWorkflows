#
# Automatically generated file; DO NOT EDIT.
# Enhanced OpenWrt Configuration with Detailed Comments
#

# ================================
# 目标架构配置
# ================================
CONFIG_TARGET_x86=y                    # 目标平台：x86架构
CONFIG_TARGET_x86_64=y                # 目标子类型：64位x86架构
CONFIG_TARGET_x86_64_DEVICE_generic=y # 设备类型：通用x86_64设备
CONFIG_HAS_SUBTARGETS=y               # 支持子目标架构
CONFIG_HAS_DEVICES=y                  # 支持具体设备型号
CONFIG_TARGET_BOARD="x86"             # 目标主板类型：x86平台
CONFIG_TARGET_SUBTARGET="64"          # 目标子架构：64位
CONFIG_TARGET_PROFILE="DEVICE_generic" # 目标配置文件：通用设备
CONFIG_TARGET_ARCH_PACKAGES="x86_64"  # 架构包类型：x86_64架构专用包
CONFIG_DEFAULT_TARGET_OPTIMIZATION="-Os -pipe" # 默认优化选项：小尺寸优化
CONFIG_CPU_TYPE=" "                   # CPU类型：自动检测
CONFIG_LINUX_6_6=y                    # Linux内核版本：6.6 LTS
CONFIG_ARCH="x86_64"                  # 架构标识：x86_64

# ================================
# 目标镜像配置
# ================================
CONFIG_TARGET_ROOTFS_EXT4FS=y         # EXT4文件系统支持：启用
CONFIG_TARGET_EXT4_RESERVED_PCT=1     # EXT4保留空间百分比：1%
CONFIG_TARGET_EXT4_BLOCKSIZE_4K=y     # EXT4块大小：4KB
CONFIG_TARGET_EXT4_BLOCKSIZE=4096     # EXT4块大小：4096字节
CONFIG_TARGET_EXT4_JOURNAL=y          # EXT4日志：启用
CONFIG_GRUB_IMAGES=y                  # GRUB引导镜像：启用
CONFIG_GRUB_EFI_IMAGES=y              # GRUB EFI镜像：启用
CONFIG_GRUB_CONSOLE=y                 # GRUB控制台：启用
CONFIG_GRUB_BAUDRATE=115200           # GRUB波特率：115200
CONFIG_GRUB_TIMEOUT="5"               # GRUB超时：5秒
CONFIG_GRUB_TITLE="OpenWrt"           # GRUB标题：OpenWrt
CONFIG_TARGET_SERIAL="ttyS0"          # 目标串口：第一串口
CONFIG_TARGET_IMAGES_GZIP=y           # 镜像压缩：GZIP格式

# ================================
# 镜像选项配置
# ================================
CONFIG_TARGET_KERNEL_PARTSIZE=100     # 内核分区大小：100MB
CONFIG_TARGET_ROOTFS_PARTSIZE=1024    # 根文件系统分区大小：1024MB
CONFIG_TARGET_ROOTFS_PARTNAME=""      # 根文件系统分区名：空
CONFIG_TARGET_ROOTFS_PERSIST_VAR=y    # 持久化/var目录：启用

# ================================
# 全局构建设置
# ================================
CONFIG_JSON_OVERVIEW_IMAGE_INFO=y     # JSON镜像信息概览：启用
CONFIG_SIGNED_PACKAGES=y              # 签名包：启用
CONFIG_SIGNATURE_CHECK=y              # 签名校验：启用
CONFIG_DOWNLOAD_CHECK_CERTIFICATE=y   # 下载证书检查：启用

# ================================
# 通用包选择
# ================================
CONFIG_PACKAGE_default-settings=y     # 默认设置：启用
CONFIG_PACKAGE_default-settings-nginx=y # Nginx默认设置：启用
CONFIG_PACKAGE_luci=y                 # LuCI Web界面：启用
CONFIG_PACKAGE_luci-mod-admin-full=y  # LuCI完整管理界面：启用
CONFIG_PACKAGE_luci-theme-bootstrap=y # Bootstrap主题：启用
CONFIG_PACKAGE_luci-theme-openwrt-2020=y # OpenWrt 2020主题：启用
CONFIG_PACKAGE_luci-app-firewall=y    # LuCI防火墙应用：启用
CONFIG_PACKAGE_luci-app-opkg=y        # LuCI包管理应用：启用
CONFIG_PACKAGE_luci-proto-ipv6=y      # LuCI IPv6协议：启用
CONFIG_PACKAGE_luci-proto-ppp=y       # LuCI PPP协议：启用

# ================================
# 系统基础包配置
# ================================
CONFIG_DEFAULT_base-files=y           # 基础文件：默认包含
CONFIG_DEFAULT_ca-bundle=y            # CA证书包：默认包含
CONFIG_DEFAULT_dnsmasq=y              # DNSmasq服务：默认包含
CONFIG_DEFAULT_dropbear=y             # Dropbear SSH：默认包含
CONFIG_DEFAULT_e2fsprogs=y            # EXT文件系统工具：默认包含
CONFIG_DEFAULT_firewall4=y            # 防火墙4：默认包含
CONFIG_DEFAULT_fstools=y              # 文件系统工具：默认包含
CONFIG_DEFAULT_grub2-bios-setup=y     # GRUB2 BIOS设置：默认包含
CONFIG_DEFAULT_kmod-amazon-ena=y      # Amazon ENA网卡驱动：默认包含
CONFIG_DEFAULT_kmod-amd-xgbe=y        # AMD XGBE网卡驱动：默认包含
CONFIG_DEFAULT_kmod-bnx2=y            # Broadcom BNX2网卡驱动：默认包含
CONFIG_DEFAULT_kmod-button-hotplug=y  # 按钮热插拔驱动：默认包含
CONFIG_DEFAULT_kmod-drm-i915=y        # Intel DRM i915驱动：默认包含
CONFIG_DEFAULT_kmod-dwmac-intel=y     # Intel DW MAC驱动：默认包含
CONFIG_DEFAULT_kmod-e1000=y           # Intel E1000网卡驱动：默认包含
CONFIG_DEFAULT_kmod-e1000e=y          # Intel E1000E网卡驱动：默认包含
CONFIG_DEFAULT_kmod-forcedeth=y        # ForceEther网卡驱动：默认包含
CONFIG_DEFAULT_kmod-fs-vfat=y         # VFAT文件系统驱动：默认包含
CONFIG_DEFAULT_kmod-igb=y             # Intel IGB网卡驱动：默认包含
CONFIG_DEFAULT_kmod-igc=y             # Intel IGC网卡驱动：默认包含
CONFIG_DEFAULT_kmod-ixgbe=y           # Intel IXGBE网卡驱动：默认包含
CONFIG_DEFAULT_kmod-nft-offload=y     # NFT卸载驱动：默认包含
CONFIG_DEFAULT_kmod-r8169=y           # Realtek R8169网卡驱动：默认包含
CONFIG_DEFAULT_kmod-tg3=y             # Tigon3网卡驱动：默认包含
CONFIG_DEFAULT_libc=y                 # C库：默认包含
CONFIG_DEFAULT_libgcc=y               # GCC库：默认包含
CONFIG_DEFAULT_libustream-mbedtls=y   # mbedtls流库：默认包含
CONFIG_DEFAULT_logd=y                 # 日志守护进程：默认包含
CONFIG_DEFAULT_mkf2fs=y               # F2FS创建工具：默认包含
CONFIG_DEFAULT_mtd=y                  # MTD工具：默认包含
CONFIG_DEFAULT_netifd=y               # 网络接口守护进程：默认包含
CONFIG_DEFAULT_nftables=y             # NFTables：默认包含
CONFIG_DEFAULT_odhcp6c=y              # ODHCP6C客户端：默认包含
CONFIG_DEFAULT_odhcpd-ipv6only=y      # ODHCPD仅IPv6：默认包含
CONFIG_DEFAULT_partx-utils=y          # Partx工具：默认包含
CONFIG_DEFAULT_ppp=y                  # PPP协议：默认包含
CONFIG_DEFAULT_ppp-mod-pppoe=y        # PPPoE模块：默认包含
CONFIG_DEFAULT_procd-ujail=y          # Procd UJail：默认包含
CONFIG_DEFAULT_uci=y                  # UCI配置接口：默认包含
CONFIG_DEFAULT_uclient-fetch=y        # UClient Fetch：默认包含
CONFIG_DEFAULT_urandom-seed=y         # 随机种子：默认包含
CONFIG_DEFAULT_urngd=y                # URNGD守护进程：默认包含

# ================================
# 硬件支持选项
# ================================
CONFIG_HAS_FPU=y                      # 硬件FPU支持：启用
CONFIG_AUDIO_SUPPORT=y                # 音频支持：启用
CONFIG_GPIO_SUPPORT=y                 # GPIO支持：启用
CONFIG_PCI_SUPPORT=y                  # PCI支持：启用
CONFIG_PCIE_SUPPORT=y                 # PCIe支持：启用
CONFIG_PCMCIA_SUPPORT=y               # PCMCIA支持：启用
CONFIG_USB_SUPPORT=y                  # USB支持：启用
CONFIG_RTC_SUPPORT=y                  # RTC支持：启用
CONFIG_VIRTIO_SUPPORT=y               # VirtIO支持：启用

# ================================
# 文件系统支持
# ================================
CONFIG_USES_SQUASHFS=y                # SquashFS文件系统：启用
CONFIG_USES_EXT4=y                    # EXT4文件系统：启用
CONFIG_USES_TARGZ=y                   # TAR Gzip文件：启用
CONFIG_ARCH_64BIT=y                   # 64位架构：启用
CONFIG_USES_ROOTFS_PART=y             # 根文件系统分区：启用
CONFIG_USES_BOOT_PART=y               # 引导分区：启用
CONFIG_x86_64=y                       # x86_64架构：启用

# ================================
# 额外的网络功能
# ================================
CONFIG_DEFAULT_kmod-usb-net=y         # USB网络驱动：默认包含
CONFIG_DEFAULT_kmod-usb-net-asix=y    # ASIX USB网卡驱动：默认包含
CONFIG_DEFAULT_kmod-usb-net-rtl8152=y # RTL8152 USB网卡驱动：默认包含
CONFIG_DEFAULT_kmod-usb2=y            # USB2驱动：默认包含
CONFIG_DEFAULT_kmod-usb3=y            # USB3驱动：默认包含
CONFIG_DEFAULT_kmod-usb-storage=y     # USB存储驱动：默认包含
CONFIG_DEFAULT_kmod-usb-storage-extras=y # USB存储扩展驱动：默认包含

# ================================
# 无线网络支持
# ================================
CONFIG_DEFAULT_hostapd=y              # HostAPD服务：默认包含
CONFIG_DEFAULT_wpad=y                 # WPAD工具：默认包含
CONFIG_DEFAULT_kmod-cfg80211=y        # cfg80211无线配置驱动：默认包含
CONFIG_DEFAULT_kmod-mac80211=y        # MAC80211无线驱动：默认包含
CONFIG_DEFAULT_kmod-ath=y             # Atheros无线驱动：默认包含
CONFIG_DEFAULT_kmod-ath9k=y           # Atheros ATH9K驱动：默认包含
CONFIG_DEFAULT_kmod-ath10k=y          # Atheros ATH10K驱动：默认包含
CONFIG_DEFAULT_kmod-rt2800-lib=y      # Ralink RT2800库：默认包含
CONFIG_DEFAULT_kmod-rt2800-pci=y      # Ralink RT2800 PCI驱动：默认包含
CONFIG_DEFAULT_kmod-rt2800-usb=y      # Ralink RT2800 USB驱动：默认包含

# ================================
# 容器和虚拟化支持
# ================================
CONFIG_DEFAULT_docker=y               # Docker引擎：默认包含
CONFIG_DEFAULT_dockerd=y              # Docker守护进程：默认包含
CONFIG_DEFAULT_containerd=y           # Containerd容器运行时：默认包含
CONFIG_DEFAULT_runc=y                 # Runc容器运行工具：默认包含
CONFIG_DEFAULT_kmod-virtio=y          # VirtIO驱动：默认包含
CONFIG_DEFAULT_kmod-virtio-blk=y      # VirtIO块设备驱动：默认包含
CONFIG_DEFAULT_kmod-virtio-console=y  # VirtIO控制台驱动：默认包含
CONFIG_DEFAULT_kmod-virtio-net=y      # VirtIO网络驱动：默认包含

# ================================
# 安全和VPN功能
# ================================
CONFIG_DEFAULT_openvpn=y              # OpenVPN：默认包含
CONFIG_DEFAULT_openvpn-easy-rsa=y     # OpenVPN Easy-RSA：默认包含
CONFIG_DEFAULT_kmod-tun=y             # TUN/TAP驱动：默认包含
CONFIG_DEFAULT_kmod-ipt-nat=y         # IPTables NAT模块：默认包含
CONFIG_DEFAULT_kmod-nf-nathelper-ct=y # NetFilter连接跟踪助手：默认包含
CONFIG_DEFAULT_kmod-udptunnel6=y      # UDP隧道IPv6：默认包含

# ================================
# 监控和性能工具
# ================================
CONFIG_DEFAULT_htop=y                 # Htop系统监控：默认包含
CONFIG_DEFAULT_iftop=y                # IfTop流量监控：默认包含
CONFIG_DEFAULT_vnstat=y               # VNStat流量统计：默认包含
CONFIG_DEFAULT_vnstati=y              # VNStat图像工具：默认包含
CONFIG_DEFAULT_iotop=y                # IOTop磁盘监控：默认包含
CONFIG_DEFAULT_dstat=y                # Dstat系统统计：默认包含
CONFIG_DEFAULT_kmod-ledtrig-heartbeat=y # LED心跳触发器：默认包含
CONFIG_DEFAULT_kmod-ledtrig-gpio=y    # LED GPIO触发器：默认包含

# ================================
# 开发和调试工具
# ================================
CONFIG_DEFAULT_gdb=y                  # GNU调试器：默认包含
CONFIG_DEFAULT_strace=y               # 系统调用跟踪：默认包含
CONFIG_DEFAULT_tcpdump=y              # 网络包捕获：默认包含
CONFIG_DEFAULT_iperf3=y               # 网络性能测试：默认包含
CONFIG_DEFAULT_screen=y               # Screen终端管理：默认包含
CONFIG_DEFAULT_tmux=y                 # Tmux终端复用：默认包含
CONFIG_DEFAULT_vim-full=y             # Vim完整编辑器：默认包含
CONFIG_DEFAULT_nano=y                 # Nano编辑器：默认包含

# ================================
# 其他实用工具
# ================================
CONFIG_DEFAULT_lxc=y                  # LXC容器：默认包含
CONFIG_DEFAULT_lxc-attach=y           # LXC连接工具：默认包含
CONFIG_DEFAULT_luci-app-docker=y      # LuCI Docker应用：默认包含
CONFIG_DEFAULT_luci-app-samba4=y      # LuCI Samba4应用：默认包含
CONFIG_DEFAULT_luci-app-upnp=y        # LuCI UPnP应用：默认包含
CONFIG_DEFAULT_luci-app-ddns=y        # LuCI DDNS应用：默认包含
CONFIG_DEFAULT_luci-app-transmission=y # LuCI Transmission应用：默认包含
CONFIG_DEFAULT_luci-app-minidlna=y    # LuCI MiniDLNA应用：默认包含